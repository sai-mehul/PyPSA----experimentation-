A simple power network simulation built with PyPSA to model economic dispatch between two generators supplying a single load.
What This Does
Simulates a single bus network where two generators  a Wind Turbine (cheap) and a Gas Turbine (expensive) are dispatched to meet a 100 MW load demand. The optimizer uses least-cost dispatch, prioritizing the cheaper generator first.
